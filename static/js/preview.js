for (let i = 2024; i <= 2024; i++) { document.getElementById('year').innerHTML += `<option value="${i}">${i}</option>`; }
for (let i = 4; i <= 4; i++) { document.getElementById('month').innerHTML += `<option value="${i}">${i}</option>`; }
for (let i = 1; i <= 31; i++) { document.getElementById('day').innerHTML += `<option value="${i}">${i}</option>`; }
for (let i = 0; i <= 23; i++) { document.getElementById('hour').innerHTML += `<option value="${i}">${i}</option>`; }
for (let i = 0; i <= 59; i++) { document.getElementById('minute').innerHTML += `<option value="${i}">${i}</option>`; }
for (let i = 0; i <= 59; i++) { document.getElementById('second').innerHTML += `<option value="${i}">${i}</option>`; }

